# robot.yaml
tasks:
  run_rsi_sma:
    shell: python -m robocorp.tasks run rsi_sma_crossover.py
  email_sender:
    shell: python -m robocorp.tasks run email_sender.py

environmentConfigs:
  - environment_windows_amd64_freeze.yaml
  - environment_linux_amd64_freeze.yaml
  - environment_darwin_amd64_freeze.yaml
  - conda.yaml

artifactsDir: output

PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore